# Extract posterior means for gamma

Extract posterior means for gamma

## Usage

``` r
get_gamma_posterior_mean(summary_df)
```

## Arguments

- summary_df:

  Summary data frame from fit\$summary()

## Value

Vector of gamma posterior means
