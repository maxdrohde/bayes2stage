% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest plot with table-style annotations}
\usage{
plot_forest(
  data,
  ref_line = 0,
  xlab = NULL,
  panel_label = NULL,
  ci_format = "\%5.2f (\%5.2f, \%4.2f)",
  se_format = "\%4.2f",
  extra_col = NULL,
  font_family = "sans",
  mono_family = "mono"
)
}
\arguments{
\item{data}{Data frame with required columns: \code{label}, \code{est}, \code{lo}, \code{hi},
\code{se}. Row types are inferred automatically:
\itemize{
\item \strong{Data row}: \code{est} is not \code{NA} -- gets point + CI + text columns.
\item \strong{Header row}: \code{est} is \code{NA} but \code{label} is non-empty -- bold,
flush-left, no CI.
\item \strong{Spacer row}: \code{label} is \code{""} or \code{NA} -- empty vertical gap.
}

Data rows appearing before the first header are rendered flush-left;
data rows after a header are indented.}

\item{ref_line}{X-intercept for vertical reference line, or \code{NULL} to omit.}

\item{xlab}{X-axis label, or \code{NULL} to omit.}

\item{panel_label}{Panel annotation (e.g., \code{"A)"}) placed top-left, or
\code{NULL} to omit.}

\item{ci_format}{\code{\link[=sprintf]{sprintf()}} format string applied to \verb{(est, lo, hi)} for
the CI text column.}

\item{se_format}{\code{\link[=sprintf]{sprintf()}} format string applied to \code{se} for the SE text
column.}

\item{extra_col}{Optional named list specifying an additional numeric column
placed between the SE and CI columns, or \code{NULL} to omit. Required elements:
\code{col} (name of the column in \code{data}), \code{format} (\code{\link[=sprintf]{sprintf()}} format string),
and \code{header} (column header text).}

\item{font_family}{Font family for labels, headers, and axis text.}

\item{mono_family}{Monospace font family for numeric columns.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Creates a forest plot with point estimates, confidence intervals, and
right-aligned numeric columns for SE and CI text, using a \code{theme_void()}
layout with a manual axis.
}
