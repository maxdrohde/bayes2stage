% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ellipse_design.R
\name{ellipse_design}
\alias{ellipse_design}
\title{Set x to missing based on an ellipse design}
\usage{
ellipse_design(
  data,
  estimation_method = c("ods", "bds"),
  fixed_effects_formula = NULL,
  cutoff_low,
  cutoff_high,
  n_sampled,
  prop_high,
  prop_middle,
  prop_low
)
}
\arguments{
\item{data}{Dataset to use}

\item{estimation_method}{How to estimate per-subject intercepts and slopes:
\code{"ods"} uses per-subject OLS, \code{"bds"} uses BLUPs from a mixed model.}

\item{fixed_effects_formula}{Formula for the fixed-effects when fitting the
model to estimate BLUPs. Required when \code{estimation_method = "bds"}.}

\item{cutoff_low}{Which quantile to use as the cutoff for the Low category}

\item{cutoff_high}{Which quantile to use as the cutoff for the High category}

\item{n_sampled}{How many subjects should be sampled?}

\item{prop_high}{What proportion to sample from the High category?}

\item{prop_middle}{What proportion to sample from the Middle category?}

\item{prop_low}{What proportion to sample from the Low category?}
}
\value{
A dataset where the x values are selected based on an ellipse design.
Includes columns \code{target} (Mahalanobis distance), \code{category},
\code{intercept}, and \code{slope}.
}
\description{
Stratifies subjects by Mahalanobis distance in the bivariate
(intercept, slope) space. Subjects far from the center (high
Mahalanobis distance) are oversampled.
}
